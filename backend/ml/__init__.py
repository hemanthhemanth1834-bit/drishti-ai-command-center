"""DRISHTI-X landslide ML package — interpretable tree-based pipeline.

Honesty contract: no metric is reported unless computed from a real training
run (see artifacts/metrics.json). Without artifacts, inference serves a
transparent DEMO heuristic explicitly labeled simulated=True.
"""
